---
interface Props {
    text: string;
    className?: string;
    variant?: "hero" | "main";
    align?: string;
}

const {
    text,
    className = "",
    variant = "main",
    align = "center",
} = Astro.props;
---

<p
    class:list={[
        "font-mont-alter",
        variant === "hero"
            ? "font-[300] text-[15px] sm:text-[20px] lg:text-[16px] 2xl:text-[18px] 3xl:text-[20px] text-white leading-6 sm:leading-7"
            : "font-[400] text-secondary text-[20px] sm:text-[24px] lg:text-[22px] xl:text-[24px] 2xl:text-[28px] 3xl:text-[32px] leading-6 sm:leading-8 xl:leading-[44px]",
        align,
        className,
    ]}
    style="font-display: swap; text-rendering: optimizeSpeed; font-smooth: always;"
>
    {text}
</p>
