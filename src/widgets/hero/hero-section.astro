---
import { Image } from "astro:assets";
import Paragraph from "../../shared/ui/paragraph/paragraph.astro";
import Button from "../../shared/ui/button/button.astro";

import Wrap from "/public/images/wrap.svg";
import Studio from "/public/images/studio.svg";
import LogoIconPc from "/public/images/logo-icon-pc.svg";
import BackgroundVideo from "/public/videos/video.mp4";
---

<style>
    .video-section {
        position: relative;
        width: 100%;
        height: 100svh;
        display: flex;
        overflow: hidden;
    }

    .video-section video {
        position: absolute;
        top: 50%;
        left: 50%;
        width: 100%;
        height: 102%;
        object-fit: cover;
        transform: translate(-50%, -50%);
        z-index: -1;
    }

    .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.9);
        z-index: 0;
    }

    .content {
        position: relative;
        z-index: 1;
        text-align: center;
    }
</style>

<main class="relative w-full">
    <section class="video-section">
        <video autoplay loop muted playsinline>
            <source src={BackgroundVideo} type="video/mp4" />
            Ваш браузер не поддерживает видео.
        </video>

        <div class="overlay"></div>

        <div
            class="content max-w-[90%] lg:max-w-[80%] 3xl:max-w-[1560px] 4xl:max-w-[1800px] mx-auto flex flex-col items-center"
        >
            <div class="lg:flex-grow"></div>
            <h1
                class="flex items-center flex-col mt-[228px] lg:flex-row lg:gap-8"
            >
                <Image
                    src={Wrap}
                    alt="Wrap"
                    class="w-[240px] sm:w-[300px] md:w-[340px] lg:hidden flex"
                    loading="eager"
                    decoding="async"
                />
                <Image
                    src={Studio}
                    alt="Studio"
                    class="w-[240px] sm:w-[300px] md:w-[340px] mt-4 lg:hidden flex"
                    loading="eager"
                    decoding="async"
                />
                <Image
                    src={LogoIconPc}
                    alt="Logo Icon"
                    class="hidden lg:flex"
                    loading="eager"
                    decoding="async"
                />
            </h1>
            <Paragraph
                variant="hero"
                text="Wrap Studio: защита вашего авто на годы. Полиуретановая пленка — броня от царапин, сколов и времени. Сохраняем идеальный вид, подчеркиваем стиль. Ваш автомобиль в надежных руках."
                className="mt-4 sm:mt-8 sm:w-[80%] md:w-[60%] lg:w-[75%] xl:w-[60%] lg:mb-28 xl:mb-36 2xl:mb-48"
            />
            <Button
                text={"Связаться с нами".toUpperCase()}
                variant="link"
                href="#contacts"
                className="mt-[64px] lg:hidden flex"
            />
        </div>
    </section>
</main>
